	<h2>Create New Contact</h2>
	

	<div class = 'large-4 columns'>
	<%= simple_form_for @contact do |f| %>
		
	  	<%= f.input :name%>
	  	<%= f.input :username%>
		<%= f.input :pass, label: 'Password' %>
	  	<%= f.button :submit, class: 'button small' %>
	<% end %>
	</div>
